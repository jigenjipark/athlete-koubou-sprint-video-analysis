# 後任者向け引き継ぎメモ

このドキュメントは、後任の開発者がコードだけを見ても分かりにくい「なぜこの実装になったか」の経緯をまとめたもの。仕様や使い方は [README.md](./README.md) を参照。

## なぜMoveNetからBlazePoseへ変更したか

初期版はTensorFlow.js + MoveNet(COCO 17キーポイント)を使用していた。しかし足首角度(「スネと足の甲・つま先方向のなす角度」= 膝-足首-つま先)を算出しようとしたところ、MoveNetの17キーポイントにはつま先(foot index)やかかとのランドマークが存在せず、正しい足首角度を計算できないことが判明した。

そのため、33ランドマークを持ちつま先(Foot Index, index 31/32)を取得できる**MediaPipe BlazePose**へ移行した。これにより `Knee - Ankle - FootIndex` の角度として正しい足首角度を算出できるようになった。

移行後もUIはMoveNet版のレイアウトをほぼそのまま踏襲している。

## なぜHeavyではなくLiteを採用したか

BlazePose移行時、最初は高精度な `pose_landmarker_heavy` を使用した。しかし動画再生中に `detectForVideo()` の推論負荷が高く、動画フレームごとに推論していたため映像がカクつく問題が発生した。

対応として、`pose_landmarker_lite` に変更し、さらに推論頻度を約20fpsに間引いた(`lastInferTime` による50ms間隔のスロットリング)。これにより動画そのものは滑らかに再生しつつ、骨格・角度の解析は20fps程度で更新する構成にしている。動画再生と解析フレームの完全な分離(全フレーム解析のオフライン処理化など)は未実装で、今後の検討課題。

## 開発の経緯(要約)

1. Python版(MediaPipe Pose + OpenCV、動画オフライン解析用)とブラウザ版(TensorFlow.js + MoveNet、リアルタイムカメラ)のMVPを作成
2. 練習現場で複数コーチが使うため、インストール不要のWeb公開方式(Vercel)を採用。URLを開くだけで使えることを最優先
3. カメラのリアルタイム解析方式から、「スマホ内の保存済み動画を選択して解析」方式へ変更(カメラ許可が不要になり運用が単純化)
4. 縦動画・横動画どちらにも対応するため、動画の実際の解像度からaspect-ratioとcanvas座標変換(scale/offX/offY)を動的に計算する構成にした
5. 足首角度の精度要求からMoveNet→BlazePoseへ移行(上記参照)
6. BlazePose Heavyのカクつき問題からLite+20fps間引きへ変更(上記参照)

## 現在の実装状態(v2.2.0)

- 単一の `index.html` にHTML/CSS/JSを統合(ビルドプロセスなし)
- BlazePose Lite、33点姿勢推定
- 骨格線・関節点描画
- 膝・肘・股関節・足首の4角度をリアルタイム表示、色分け済み
- ローカル動画ファイル選択方式(カメラ機能は削除済み)
- 独自カスタム動画コントローラ(標準video controlsはcanvas重畳時にスマホでスライダーが消えることがあったため自前実装)
- 一時停止中はシーク/コマ送り時のみそのフレームを再推論して描画

## 既知の課題・注意点

1. **1フレーム送りが30fps固定** — `const FRAME = 1/30;` としているため、60fps/120fps動画では正確な1フレーム送りにならない。短距離走では120fps動画を使う可能性が高く、優先度の高い改善候補。
2. **高fps動画での解析取りこぼし** — 推論を20fps程度に間引いているため、高fps動画を通常速度で再生すると全フレームは解析されない。「動画再生」と「解析フレーム」を分離する設計(例: 一時停止してのフレーム単位解析、またはオフラインでの事前解析)を検討中。
3. データの永続化・共有は未実装。現状はブラウザ内で完結し、CSV出力・画像保存・サーバ保存はまだ無い。

## リポジトリ運用上の注意

- これまでのコミットはすべてGitHub Web UIからの「Add files via upload」で、ローカルgit/CLIでのpushではない。今後CLIでpushする場合は既存の単一ファイル構成と履歴に注意する。
- Vercel等に接続して自動デプロイする場合、`index.html` の更新がそのまま本番に反映されるため、変更前の動作確認を徹底すること。

## 今後の開発候補(優先度順)

1. 動画の実FPS取得または設定機能、60fps/120fps対応の正確な1フレーム送り
2. CSV出力(タイムスタンプ・各関節角度・レップ番号など)
3. 選手名・練習日・試技番号などのメタ情報入力
4. 静止画(スクリーンショット)保存
5. 角度の時間推移グラフ表示
6. 2動画比較機能(横並び・同期再生・角度比較)
7. 選手ごとのデータ管理・チーム共有(Render等のバックエンド+DB追加を想定)
8. PWA化

より詳しい過去の検討経緯は `sprint_video_analysis/work-memo/` (開発者手元の引き継ぎメモ、memo1〜memo4)にも記録されている。
